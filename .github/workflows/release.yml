name: Release

on:
  release:
    types: [created]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_REGISTRIES_CRATES_IO_PROTOCOL: sparse
  CARGO_NET_GIT_FETCH_WITH_CLI: true

jobs:
  build:
    name: Build release binary
    # Self-hosted macOS runner required for CommonCrypto FFI
    runs-on: spiceai-macos
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release --locked --bin spiceio

      - name: Package binary
        # Bundle the dSYM alongside the (stripped) binary so a production crash
        # report's raw addresses can be symbolized offline against this exact
        # build: `atos -o spiceio.dSYM/Contents/Resources/DWARF/spiceio
        # -l <image base from the report> <addrs...>`.
        run: |
          cd target/release
          tar czf spiceio-${{ runner.os }}-${{ runner.arch }}.tar.gz spiceio spiceio.dSYM
          shasum -a 256 spiceio-${{ runner.os }}-${{ runner.arch }}.tar.gz > spiceio-${{ runner.os }}-${{ runner.arch }}.tar.gz.sha256

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" --clobber \
            target/release/spiceio-${{ runner.os }}-${{ runner.arch }}.tar.gz \
            target/release/spiceio-${{ runner.os }}-${{ runner.arch }}.tar.gz.sha256
